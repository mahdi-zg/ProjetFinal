<div class="panel panel-default">
<div class="panel-heading">Les Statistiques des Utilisateurs</div>
 <div class="panel-body" style="padding:15px;">
 <table class="table table-striped">
 <thead>
    <th>les derniers modfications sur le profil </th>
    <th>Tout les utilisateurs </th>
    <th>jour de Creation des derniers Compte </th>
    <th>Users&Admin </th>
  </thead>  

  <tbody>
                        <tr>
                            <td style="border: 1px #88889b solid"><%= line_chart User.group_by_day(:created_at, range: 1.week.ago..Time.now).sum(:updated_at) %>
</td>
                            <td  style="border: 1px #88889b solid"><%= pie_chart User.group(:username).count %>
</td>
                            <td  style="border: 1px #88889b solid"><%= bar_chart User.group_by_day(:created_at).count(:updated_at) %>
</td>
                            <td style="border: 1px #88889b solid"><%= line_chart User.group(:role).group(:username).count %>
 </td>
                        </tr>

                        

                    </tbody>
 </table>
 
        <table  id="example" class="table table-striped table-bordered"> 
                    <h2>Les Questions dédier Pour les Utilisateurs</h2>

                    <thead>
                        <th>Titre</th> 
                        <th>Question</th> 
                        <th>Actions</th> 
                        </tr>
                    </thead>

                    <tbody>
                        <% @species.each do |species| %>

                        <tr>
                            <td><%= species.name%></td>
                            <td><%= species.slug%></td>
                        <td>
                        <%= link_to 'Editer', [:edit, :admin, species], class: 'btn btn-primary'%>
                        <%= link_to 'Supprimer', [:admin, species], class: 'btn btn-danger',data:{method: :destroy, Confirm:'Voulez vous Supprimer?'} %>
                        </td>
                        </tr>

                        
                        <% end %>
                    <div class="" style ="padding:6px;" >
                        <%= link_to 'Ajouter une Nouvelle Question ', "/superadmin/species/new", class: 'btn btn-success' %>
                    </div>
                    </tbody>

    
        </table>
        

</div>